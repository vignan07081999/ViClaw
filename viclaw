#!/bin/bash
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
if [ -f "$DIR/.venv/bin/python3" ]; then
    exec "$DIR/.venv/bin/python3" "$DIR/viclaw_router.py" "$@"
else
    echo "ViClaw virtual environment not found. Please run ./install.sh first."
    exit 1
fi
